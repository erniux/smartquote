# pytest.ini
[pytest]
addopts = -q
testpaths = tests
bdd_features_base_dir = tests/features
bdd_strict_gherkin = true
markers =
    ui: pruebas de interfaz con Playwright
    api: pruebas de API (request context)
    smoke: subset rápido
    slow: pruebas lentas
